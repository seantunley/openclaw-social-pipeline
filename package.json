{
  "name": "social-pipeline-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Social media content pipeline — Telegram bot operator + React dashboard reviewer, sharing one engine (research, drafting, SEO/GEO, humanizer, media gen, Postiz publishing)",
  "workspaces": [
    "engine",
    "dashboard/social-pipeline-dashboard"
  ],
  "scripts": {
    "dev": "concurrently -n api,dashboard -c blue,green \"npm run dev:api\" \"npm run dev:dashboard\"",
    "dev:api": "cd engine && npm run start:api",
    "dev:bot": "cd engine && npm run start:bot",
    "dev:dashboard": "cd dashboard/social-pipeline-dashboard && npm run dev",
    "build": "npm run build:engine && npm run build:dashboard",
    "build:engine": "cd engine && npm run build",
    "build:dashboard": "cd dashboard/social-pipeline-dashboard && npm run build",
    "install:all": "npm install && cd engine && npm install && cd ../dashboard/social-pipeline-dashboard && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "overrides": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.78",
    "ai": "^6.0.184"
  }
}
